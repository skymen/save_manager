export const config = {
  highlight: false,
  isDeprecated: false,
  isTrigger: true,
  listName: "On delete error",
  displayText: "On delete error",
  description: "Triggered when deleting the save fails",
  params: [],
};

export const expose = true;

export default function () {
  return true;
}
